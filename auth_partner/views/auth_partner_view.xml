<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="auth_partner_view_tree" model="ir.ui.view">
        <field name="model">auth.partner</field>
        <field name="arch" type="xml">
            <tree>
                <field name="directory_id" />
                <field name="partner_id" />
                <field name="login" />
                <field name="mail_verified" />
                <field name="nbr_pending_reset_sent" />
                <field name="date_last_request_reset_pwd" />
                <field name="date_last_sucessfull_reset_pwd" />
            </tree>
        </field>
    </record>
    <record id="auth_partner_view_form" model="ir.ui.view">
        <field name="model">auth.partner</field>
        <field name="arch" type="xml">
            <form string="Auth Partner">
                <header>
                    <button name="send_invite" type="object" string="Send Invite" />
                    <button name="impersonate" type="object" string="Impersonate" class="btn-info" attrs="{'invisible': [('user_can_impersonate', '=', False)]}" />
                    <button name="%(auth_partner_force_set_password_action)d" type="action" string="Set Password" class="btn-secondary" groups="auth_partner.group_auth_partner_manager" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="user_can_impersonate" invisible="1" />
                            <field name="partner_id" />
                        </h1>
                    </div>
                    <group>
                        <field name="login" />
                        <field name="directory_id" />
                    </group>
                    <group>
                        <field name="mail_verified" />
                        <field name="nbr_pending_reset_sent" />
                        <field name="date_last_request_reset_pwd" />
                        <field name="date_last_sucessfull_reset_pwd" />
                        <field name="date_last_impersonation" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
